export const SMART_MAX_WIDTH = 1200;
export const DESKTOP_MIN_WIDTH = SMART_MAX_WIDTH + 1;

export const SMART_QUERY = `(max-width: ${SMART_MAX_WIDTH}px)`;
export const DESKTOP_QUERY = `(min-width: ${DESKTOP_MIN_WIDTH}px)`;
