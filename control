#!/bin/sh
set -eu

script_dir=$(CDPATH= cd -- "$(dirname -- "$0")" && pwd)
if [ -x "$script_dir/.venv/bin/python" ]; then
  python_command="$script_dir/.venv/bin/python"
else
  python_command=python3
fi
exec "$python_command" "$script_dir/tools/control.py" "$@"
