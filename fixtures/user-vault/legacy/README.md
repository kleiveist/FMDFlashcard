# Legacy user-vault fixture

This directory contains deterministic, synthetic data for migration tests. It
matches the former `UserGlobal` layout but contains no real user names, vault
paths, learning history, or identifiers.

The fixture is repository-owned test data. Runtime user data must remain
outside the repository and must never be copied into this directory.
